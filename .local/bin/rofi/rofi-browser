#!/bin/sh
#
# Choose a bookmark, or search query and run browser.
# Requires rofi.

SELECTION=$(cat "$BOOKMARKS" | rofi -dmenu -p "Search/Bookmark") && $BROWSER "$(echo "$SELECTION" | cut -d ' ' -f 1)" > /dev/null 2>&1
